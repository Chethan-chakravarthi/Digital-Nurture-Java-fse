# Spring REST All HandsOn

Spring Boot REST project using XML-configured employees and departments.

## Endpoints

- `GET /employees`
- `GET /departments`

## Run

Start `com.chethan.springrest.SpringRestApplication` on port `8090`.