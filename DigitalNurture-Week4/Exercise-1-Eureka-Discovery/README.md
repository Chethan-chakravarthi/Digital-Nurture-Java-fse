# Exercise 1 - Eureka Discovery Server

This project contains a simple Spring Cloud Eureka Server that can be used as the service registry for microservices.

## Run the application

From this folder, run:

```bash
mvn spring-boot:run
```

Then open:

```text
http://localhost:8761
```

You should see the Eureka dashboard.
